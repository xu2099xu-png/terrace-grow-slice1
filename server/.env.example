DATABASE_URL="postgresql://terrace:terrace@localhost:5433/terrace_grow?schema=public"
JWT_SECRET="replace-with-a-strong-secret-in-production"
JWT_EXPIRES_IN="365d"
PORT=3000
APP_ENV=development
CORS_ORIGINS="http://localhost:5173"
# DEV ONLY: allow draft fixture data into engines. NOT approved agricultural content.
# Must be explicitly set to 'true' AND APP_ENV=development to allow draft data.
ALLOW_DRAFT_FIXTURES=true

# Optional real provider configuration. Without these, seasonal weather
# gracefully reports unavailable and location falls back to manual city choice.
LOCATION_RESOLVER="http"
WEATHER_PROVIDER="http"
QWEATHER_API_HOST="your-project.qweatherapi.com"
QWEATHER_KEY=""
LOCATION_API_KEY=""
WEATHER_PROVIDER_TIMEOUT_MS=3500

# Process-local limiter settings for the current single-API-replica topology.
RATE_LIMIT_GLOBAL_LIMIT=300
RATE_LIMIT_TTL_MS=60000

# Development/test-only deterministic season override. Forbidden in production.
SEASON_DATE=""

# Slice 5 grounded AI explanation. Default off is valid in every environment.
# mock is forbidden in production. openai_compatible requires every AI_* value
# below and calls ${AI_PROVIDER_BASE_URL}/chat/completions from the server only.
AI_PROVIDER="off"
AI_PROVIDER_BASE_URL=""
AI_PROVIDER_API_KEY=""
AI_PROVIDER_MODEL=""
AI_PROVIDER_TIMEOUT_MS=5000
AI_PROMPT_VERSION="slice5-v1"
AI_EXPLANATION_CACHE_TTL_SECONDS=86400
AI_DAILY_PROVIDER_CALL_CAP=20
AI_ENDPOINT_LIMIT=20
AI_ENDPOINT_TTL_MS=60000
